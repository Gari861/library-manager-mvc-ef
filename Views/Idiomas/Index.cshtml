@model IEnumerable<WebAppLibros.Models.Idioma>

@{
    ViewData["Title"] = "Index";
}

<h1>IDIOMAS DE LIBROS</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdIdioma">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdIdioma">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdIdioma">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
